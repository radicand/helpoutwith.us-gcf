query permitActivityUserRoleCreate($user_id: ID!, $input_activityId: ID!) {
  SomeActivityUserRoleExists(
    filter: { activity: { id: $input_activityId, members_some: { role: Admin, user: { id: $user_id } } } }
  )
}
